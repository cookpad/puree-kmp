package com.cookpad.puree

/**
 * Builder class for creating and configuring a [PureeLogger] instance.
 *
 * This class provides methods for configuring filters and outputs for different log types,
 * and a build method for creating a PureeLogger with the configured settings.
 *
 * Platform-specific implementations may have different constructor parameters and methods
 * for registering log types, filters, and outputs.
 */
expect class Puree
